{
  "name": "plasma-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "cd frontend && pnpm run build",
    "install": "cd frontend && pnpm install"
  }
}
