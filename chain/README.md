# BSC Payment Channel Demo

这是一个基于 BSC 网络的支付通道演示项目，允许用户存款和提现 BNB。项目包含智能合约和前端界面。

## 功能特点

- **存款功能**: 用户可以向智能合约存入 BNB
- **提现功能**: 用户可以从智能合约中提取 BNB
- **余额查询**: 实时查看用户在合约中的余额
- **交易记录**: 前端界面显示所有交易日志
- **钱包集成**: 支持 MetaMask 钱包连接

## 项目结构

```
.
├── contracts/              # 智能合约目录
│   ├── Counter.sol        # 示例合约
│   └── PaymentChannel.sol # 支付通道合约
├── ignition/              # 部署配置
│   └── modules/
│       └── PaymentChannel.ts
├── test/                  # 测试文件
│   ├── Counter.ts
│   └── PaymentChannel.ts
├── frontend/              # 前端文件
│   ├── index.html         # 主页面
│   └── app.js             # 前端逻辑
├── hardhat.config.ts      # Hardhat 配置
├── package.json           # 项目依赖
└── README.md              # 项目说明
```

## 快速开始

### 环境要求

- Node.js (v18+)
- npm 或 yarn
- MetaMask 浏览器扩展

### 安装依赖

```bash
npm install
```

### 编译智能合约

```bash
npx hardhat compile
```

### 运行测试

```bash
npx hardhat test
```

### 部署到本地网络

1. 启动本地 Hardhat 节点：

```bash
npx hardhat node --hostname 127.0.0.1 --port 8546
```

2. 在新终端中部署合约：

```bash
npx hardhat ignition deploy ignition/modules/PaymentChannel.ts --network localhost
```

部署成功后，记下合约地址（当前部署：USDT `0x68B1D87F95878fE05B998F19b66F4baba5De1aed`，PaymentChannel `0x3Aa5ebB10DC797CAC828524e59A333d0A371443c`）。

### 部署到 BSC 测试网

1. 编辑 `.env` 文件，设置你的 BSC 测试网配置：

```bash
# 打开 .env 文件进行编辑
# 设置 BSC 测试网 RPC URL（已预设）
BSC_TESTNET_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545

# 设置你的私钥（不带 0x 前缀）
# 获取私钥方法：
# 1. 从 MetaMask 导出测试账户私钥
# 2. 或使用命令生成：node -e "console.log(require('ethers').Wallet.createRandom().privateKey)"
BSC_TESTNET_PRIVATE_KEY=8a36f172cf4e341d9bb318e5f226c2ddaddbe793ee134144762fb12239f91ee0
```

2. **获取测试 BNB**（必需）：
   - 访问 [BSC Testnet Faucet](https://testnet.binance.org/faucet-smart)
   - 连接你的钱包（切换到 BSC 测试网）
   - 输入你的钱包地址（对应上面私钥的地址）
   - 获取测试 BNB（每次最多 0.5 BNB）

3. 部署到 BSC 测试网：

```bash
npx hardhat ignition deploy ignition/modules/PaymentChannel.ts --network bscTestnet
```

**注意**：部署需要支付 gas 费用，确保账户有足够的测试 BNB。

### 运行前端

1. 启动前端服务器：

```bash
cd frontend
python3 -m http.server 8080
```

或者使用任何 HTTP 服务器。

2. 打开浏览器访问：`http://localhost:8080`

3. 配置 MetaMask：
   - 连接到本地网络（localhost:8546）
   - 网络名称：Hardhat
   - RPC URL：`http://127.0.0.1:8546`
   - 链 ID：31337
   - 货币符号：ETH（本地网络使用 ETH 而非 BNB）

4. 导入测试账户到 MetaMask：
   - 使用 Hardhat 节点启动时显示的私钥（如 `0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80`）
   - 该账户有 10000 ETH 测试币

## 使用指南

### 连接钱包

1. 点击 "Connect MetaMask" 按钮
2. 在 MetaMask 弹出窗口中授权连接
3. 成功连接后，页面会显示你的账户地址和网络信息

### 存款 BNB

1. 在 "Deposit BNB" 部分输入存款金额
2. 点击 "Deposit" 按钮
3. 在 MetaMask 中确认交易
4. 交易确认后，你的余额会自动更新

### 提现 BNB

1. 在 "Withdraw BNB" 部分输入提现金额
2. 点击 "Withdraw" 按钮
3. 在 MetaMask 中确认交易
4. 交易确认后，你的余额会自动更新

### 刷新余额

点击 "Refresh Balance" 按钮手动更新余额显示。

## 合约说明

### PaymentChannel.sol

主要功能函数：

- `deposit()`: 存款函数，接收 BNB 并更新用户余额
- `withdraw(uint256 amount)`: 提现函数，提取指定金额的 BNB
- `balances(address)`: 查看指定地址的余额
- `getBalance(address)`: 获取指定地址的余额

事件：
- `Deposited(address indexed user, uint256 amount)`: 存款事件
- `Withdrawn(address indexed user, uint256 amount)`: 提现事件

### 安全性考虑

1. **重入攻击防护**: 使用 Checks-Effects-Interactions 模式
2. **输入验证**: 所有输入都进行有效性检查
3. **余额检查**: 提现时确保用户有足够余额

## 测试覆盖

项目包含完整的测试套件：

- 存款功能测试
- 提现功能测试
- 余额不足测试
- 事件发射测试

运行 `npx hardhat test` 查看测试结果。

## 部署到生产环境

### BSC 主网部署

1. 在 `.env` 文件中添加 BSC 主网配置：

```
BSC_MAINNET_RPC_URL=https://bsc-dataseed.binance.org/
BSC_MAINNET_PRIVATE_KEY=你的主网私钥
```

**⚠️ 安全警告**: 主网私钥涉及真实资金，请确保：
- 使用专门的部署账户，不要使用存储大量资金的账户
- 确保 `.env` 文件不被提交到版本控制
- 考虑使用硬件钱包或多签钱包进行部署

2. 确保账户有足够的 BNB 支付 gas 费用（建议至少 0.1 BNB）

3. 部署到 BSC 主网：

```bash
npx hardhat ignition deploy ignition/modules/PaymentChannel.ts --network bscMainnet
```

部署成功后，记录合约地址。

### 使用真实钱包和合约地址

#### 前端配置
1. 访问前端界面：`http://localhost:8080`
2. 在 "Contract Info" 部分的 "Set Contract Address" 输入框中输入你的真实合约地址
3. 点击 "Update Contract" 按钮保存
4. 连接你的真实钱包（MetaMask）
5. 确保 MetaMask 连接到 BSC 主网：
   - 网络名称：Binance Smart Chain
   - RPC URL: https://bsc-dataseed.binance.org/
   - 链 ID: 56
   - 货币符号: BNB

#### 功能测试
- **存款**: 使用真实 BNB 进行存款测试（建议先使用小金额）
- **提现**: 测试从合约中提取 BNB
- **余额查询**: 实时查看合约中的余额

#### 安全建议
1. **分批测试**: 先使用最小金额（如 0.001 BNB）测试所有功能
2. **合约审计**: 在生产环境使用前，建议进行专业的安全审计
3. **备份**: 保存合约源代码、ABI 和部署地址
4. **监控**: 部署后监控合约的交易和事件

**注意**: 部署到主网，后所有交易都是不可逆的，请谨慎操作。

## ✅ 部署成功后的操作

恭喜！您的合约已成功部署到 BSC 主网。

### 📋 部署信息
- **合约地址**: `0xDC8eAbca4665b57Aae33d99394b92D0c9C3AF39b`
- **部署网络**: BSC 主网 (链 ID: 56)
- **部署账户**: `0xDDD41A9b717088Da9E1ea6ba618e73c9301DdAE3`
- **查看合约**: https://bscscan.com/address/0xDC8eAbca4665b57Aae33d99394b92D0c9C3AF39b

### 🚀 快速开始

1. **启动前端**:
   ```bash
   # 方法一：使用启动脚本
   chmod +x start-frontend.sh
   ./start-frontend.sh

   # 方法二：手动启动
   cd frontend
   python3 -m http.server 8080
   ```

2. **访问前端**: http://localhost:8080

3. **配置合约地址**:
   - 在 "Contract Info" 部分输入合约地址: `0xDC8eAbca4665b57Aae33d99394b92D0c9C3AF39b`
   - 点击 "Update Contract" 保存

4. **配置 MetaMask**:
   - 确保 MetaMask 连接到 **BSC 主网** (Binance Smart Chain)
   - 链 ID: 56
   - RPC URL: https://bsc-dataseed.binance.org/

5. **连接钱包**: 点击 "Connect MetaMask"

6. **测试功能** (使用最小金额):
   - **存款**: 存入 0.0001 BNB 测试
   - **提现**: 提取 0.00005 BNB 测试
   - **余额查询**: 点击 "Refresh Balance"

### 📁 详细操作指南
完整的部署后操作指南请查看 [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)。

### 🔧 部署文件位置
- 合约地址记录: `ignition/deployments/chain-56/deployed_addresses.json`
- 合约 ABI: `frontend/js/abi/PaymentChannel.json`
- 环境配置: `.env` (已配置主网私钥)

### ⚠️ 安全提醒
1. **小额测试**: 先使用 0.0001 BNB 测试所有功能
2. **余额监控**: 账户剩余约 0.002875 BNB，请合理使用
3. **交易确认**: 所有交易都需要 MetaMask 确认
4. **网络确认**: 始终确认连接到 BSC 主网 (链 ID: 56)

### 🔍 验证部署
1. 在 BscScan 上查看合约: https://bscscan.com/address/0xDC8eAbca4665b57Aae33d99394b92D0c9C3AF39b
2. 确认合约有正确的字节码
3. 监控合约的交易记录

## 故障排除

### 常见问题

1. **MetaMask 连接失败**
   - 确保 MetaMask 已安装并解锁
   - 检查是否已授权连接

2. **交易被拒绝**
   - 确保账户有足够余额支付 gas 费用
   - 检查网络配置是否正确

3. **配置变量未找到错误** (Error HHE7: Configuration Variable "XXX" not found)
   - 确保 `.env` 文件已正确配置，变量名与 `hardhat.config.ts` 中一致
   - 检查 `.env` 文件是否在项目根目录
   - 确认已安装 `dotenv` 依赖并正确导入

4. **资金不足错误** (Error HHE10407: Account has insufficient funds)
   - 部署到测试网需要测试币支付 gas 费用
   - 从 BSC 测试网水龙头获取测试 BNB
   - 确保私钥对应的账户有足够余额

5. **网络配置验证错误** (Error HHE15: Invalid config)
   - 检查 `hardhat.config.ts` 中的网络配置格式
   - 确保 RPC URL 格式正确且可访问
   - 确认私钥格式正确（不带 0x 前缀）

6. **合约调用失败**
   - 确认合约地址正确
   - 检查是否在正确的网络上

7. **前端无法加载**
   - 确保 HTTP 服务器正在运行
   - 检查浏览器控制台是否有错误

### 获取测试网 BNB

如需在 BSC 测试网进行测试，可以从 BSC 测试网水龙头获取测试币：

#### 官方水龙头
- https://testnet.binance.org/faucet-smart

#### 常见问题

**错误：** `Request Failed, insufficient BNB on BSC mainnet (require >=0.002BNB)`

**原因：** 某些水龙头为了防止机器人滥用，要求用户在 BSC 主网上至少有 0.002 BNB 才能领取测试币。

**解决方案：**

1. **使用本地网络测试**（推荐）：
   - 无需测试币，使用 Hardhat 本地网络
   - 配置 MetaMask 连接到 `http://127.0.0.1:8546`
   - 使用提供的测试账户私钥
   - 所有功能完全可用

2. **尝试其他水龙头**：
   - [BSC Testnet Faucet 2](https://testnet.bnbchain.org/faucet)（可能需要 Discord 验证）
   - [Third-party faucets](https://www.bnbchain.org/en/testnet-faucet)（搜索其他社区水龙头）

3. **获取少量主网 BNB**：
   - 从交易所购买少量 BNB 并转移到你的钱包
   - **注意**：仅建议在确实需要测试网部署时使用此方法

4. **使用其他测试网络**：
   - 考虑使用 Sepolia（以太坊测试网），水龙头更易获取
   - 修改配置使用 Sepolia 网络

#### 本地测试配置

如果遇到水龙头问题，建议使用本地网络测试：
1. 确保本地 Hardhat 节点运行：`npx hardhat node --hostname 127.0.0.1 --port 8546`
2. 部署合约到本地网络：`npx hardhat ignition deploy ignition/modules/PaymentChannel.ts --network localhost`
3. 前端自动使用本地合约地址
4. 在 MetaMask 中导入测试账户私钥：`0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80`

## 贡献指南

1. Fork 本项目
2. 创建功能分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m 'Add some amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing-feature`)
5. 打开 Pull Request

## 许可证

ISC License

## 免责声明

本项目仅用于演示和教育目的。在生产环境中使用前，请进行充分的安全审计和测试。作者不对任何资金损失负责。